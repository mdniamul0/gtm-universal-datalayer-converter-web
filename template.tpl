___TERMS_OF_SERVICE___

By creating or modifying this file you agree to Google Tag Manager's Community
Template Gallery Developer Terms of Service available at
https://developers.google.com/tag-manager/gallery-tos (or such other URL as
Google may provide), as modified from time to time.


___INFO___

{
  "type": "MACRO",
  "id": "cvt_temp_public_id",
  "version": 1,
  "securityGroups": [],
  "displayName": "Universal Data Layer Converter by MD Niamul",
  "categories": [
    "CONVERSIONS",
    "ANALYTICS",
    "UTILITY"
  ],
  "brand": {
    "id": "brand_dummy",
    "displayName": "MD Niamul",
    "thumbnail": "data:image/png;base64,"
  },
  "description": "Reads the website data layer whatever structure it uses, detects the available fields, and returns them in the format required by GA4, Google Ads, Meta, TikTok, LinkedIn, Snapchat, Pinterest, Microsoft Ads and other platforms.",
  "containerContexts": [
    "WEB"
  ]
}

___TEMPLATE_PARAMETERS___

[
  {
    "type": "SELECT",
    "name": "sourceMode",
    "displayName": "Data source",
    "macrosInSelect": false,
    "selectItems": [
      {
        "value": "auto",
        "displayValue": "Read the data layer automatically"
      },
      {
        "value": "variable",
        "displayValue": "Use an object from another variable"
      }
    ],
    "simpleValueType": true,
    "defaultValue": "auto",
    "help": "Automatic mode reads the current data layer state for the keys listed below. Use the second option to pass in a Data Layer Variable or a Custom JavaScript Variable that already returns the object."
  },
  {
    "type": "TEXT",
    "name": "sourceObject",
    "displayName": "Source object variable",
    "simpleValueType": true,
    "help": "A variable that returns an object, for example <code>{{DLV - ecommerce}}</code>.",
    "enablingConditions": [
      {
        "paramName": "sourceMode",
        "paramValue": "variable",
        "type": "EQUALS"
      }
    ],
    "valueValidators": [
      {
        "type": "NON_EMPTY"
      }
    ]
  },
  {
    "type": "SIMPLE_TABLE",
    "name": "extraKeys",
    "displayName": "Extra data layer keys to read",
    "help": "The template already reads the common keys (event, ecommerce, items, user_data, customer, order, transaction, page, cart, product, value, currency, transaction_id). Add any custom top-level keys your site uses.",
    "newRowButtonText": "Add key",
    "enablingConditions": [
      {
        "paramName": "sourceMode",
        "paramValue": "auto",
        "type": "EQUALS"
      }
    ],
    "simpleTableColumns": [
      {
        "defaultValue": "",
        "displayName": "Data layer key",
        "name": "key",
        "type": "TEXT",
        "valueValidators": [
          {
            "type": "NON_EMPTY"
          }
        ],
        "isUnique": true
      }
    ]
  },
  {
    "type": "SELECT",
    "name": "platform",
    "displayName": "Output format (platform)",
    "macrosInSelect": false,
    "selectItems": [
      {
        "value": "normalized",
        "displayValue": "Universal / Normalized object"
      },
      {
        "value": "ga4",
        "displayValue": "Google Analytics 4"
      },
      {
        "value": "google_ads",
        "displayValue": "Google Ads"
      },
      {
        "value": "meta",
        "displayValue": "Meta (Facebook) Pixel & CAPI"
      },
      {
        "value": "tiktok",
        "displayValue": "TikTok Pixel & Events API"
      },
      {
        "value": "linkedin",
        "displayValue": "LinkedIn Insight & Conversions API"
      },
      {
        "value": "snapchat",
        "displayValue": "Snapchat Pixel & CAPI"
      },
      {
        "value": "pinterest",
        "displayValue": "Pinterest Tag & Conversions API"
      },
      {
        "value": "microsoft",
        "displayValue": "Microsoft Advertising (Bing UET)"
      },
      {
        "value": "twitter",
        "displayValue": "X (Twitter) Ads"
      },
      {
        "value": "reddit",
        "displayValue": "Reddit Ads"
      },
      {
        "value": "quora",
        "displayValue": "Quora Ads"
      },
      {
        "value": "outbrain",
        "displayValue": "Outbrain"
      },
      {
        "value": "taboola",
        "displayValue": "Taboola"
      },
      {
        "value": "openai",
        "displayValue": "OpenAI Ads (generic structure)"
      },
      {
        "value": "stape",
        "displayValue": "Stape / server-side pass-through"
      }
    ],
    "simpleValueType": true,
    "defaultValue": "normalized",
    "help": "The platform whose structure the data layer should be converted into. Choose <b>Universal / Normalized object</b> to get the platform-independent model."
  },
  {
    "type": "SELECT",
    "name": "outputMode",
    "displayName": "Return",
    "macrosInSelect": false,
    "selectItems": [
      {
        "value": "object",
        "displayValue": "Full converted object"
      },
      {
        "value": "event",
        "displayValue": "Event name only"
      },
      {
        "value": "field",
        "displayValue": "Single field from the converted object"
      },
      {
        "value": "json",
        "displayValue": "Full converted object as a JSON string"
      }
    ],
    "simpleValueType": true,
    "defaultValue": "object",
    "help": "What this variable returns. Use <b>Single field</b> to read one key, for example <code>custom_data.value</code>."
  },
  {
    "type": "TEXT",
    "name": "outputPath",
    "displayName": "Field path",
    "simpleValueType": true,
    "help": "Dot notation path inside the converted object, for example <code>custom_data.content_ids</code> or <code>params.items.0.item_id</code>.",
    "valueHint": "custom_data.value",
    "enablingConditions": [
      {
        "paramName": "outputMode",
        "paramValue": "field",
        "type": "EQUALS"
      }
    ],
    "valueValidators": [
      {
        "type": "NON_EMPTY"
      }
    ]
  },
  {
    "type": "SELECT",
    "name": "missingMode",
    "displayName": "When a value is not found",
    "macrosInSelect": false,
    "selectItems": [
      {
        "value": "omit",
        "displayValue": "Leave the key out (recommended)"
      },
      {
        "value": "empty_string",
        "displayValue": "Return an empty string"
      },
      {
        "value": "placeholder",
        "displayValue": "Return a placeholder text"
      }
    ],
    "simpleValueType": true,
    "defaultValue": "omit",
    "help": "No value is ever invented. This only decides how a missing value is represented."
  },
  {
    "type": "TEXT",
    "name": "placeholderValue",
    "displayName": "Placeholder text",
    "simpleValueType": true,
    "defaultValue": "Not Available",
    "enablingConditions": [
      {
        "paramName": "missingMode",
        "paramValue": "placeholder",
        "type": "EQUALS"
      }
    ]
  },
  {
    "type": "GROUP",
    "name": "advancedGroup",
    "displayName": "Advanced",
    "groupStyle": "ZIPPY_CLOSED",
    "subParams": [
      {
        "type": "SIMPLE_TABLE",
        "name": "manualMapping",
        "displayName": "Manual mapping (overrides automatic detection)",
        "help": "Use this when automatic detection picks the wrong value. Left: the canonical field. Right: the exact path in the source data, for example <code>ecommerce.purchase.actionField.id</code> or <code>customer.contact.email</code>. Important: the path is resolved against the same data this template already reads — in \"Auto\" source mode that means the built-in default keys plus anything you listed under \"Extra data layer keys to read\" above; a top-level key that is not read is not available here. In \"Variable\" source mode the full object you selected is available. If a mapping returns nothing, add the missing top-level key to \"Extra data layer keys to read\" first.",
        "newRowButtonText": "Add mapping",
        "simpleTableColumns": [
          {
            "defaultValue": "value",
            "displayName": "Field",
            "name": "field",
            "type": "SELECT",
            "selectItems": [
              {
                "value": "event",
                "displayValue": "event"
              },
              {
                "value": "currency",
                "displayValue": "currency"
              },
              {
                "value": "value",
                "displayValue": "value"
              },
              {
                "value": "transaction_id",
                "displayValue": "transaction_id"
              },
              {
                "value": "tax",
                "displayValue": "tax"
              },
              {
                "value": "shipping",
                "displayValue": "shipping"
              },
              {
                "value": "coupon",
                "displayValue": "coupon"
              },
              {
                "value": "search_term",
                "displayValue": "search_term"
              },
              {
                "value": "event_id",
                "displayValue": "event_id"
              },
              {
                "value": "item_list_name",
                "displayValue": "item_list_name"
              },
              {
                "value": "user.email",
                "displayValue": "user.email"
              },
              {
                "value": "user.phone",
                "displayValue": "user.phone"
              },
              {
                "value": "user.first_name",
                "displayValue": "user.first_name"
              },
              {
                "value": "user.last_name",
                "displayValue": "user.last_name"
              },
              {
                "value": "user.street",
                "displayValue": "user.street"
              },
              {
                "value": "user.city",
                "displayValue": "user.city"
              },
              {
                "value": "user.region",
                "displayValue": "user.region"
              },
              {
                "value": "user.postal_code",
                "displayValue": "user.postal_code"
              },
              {
                "value": "user.country",
                "displayValue": "user.country"
              },
              {
                "value": "user.external_id",
                "displayValue": "user.external_id"
              },
              {
                "value": "page.page_location",
                "displayValue": "page.page_location"
              },
              {
                "value": "page.page_title",
                "displayValue": "page.page_title"
              },
              {
                "value": "page.page_referrer",
                "displayValue": "page.page_referrer"
              }
            ],
            "valueValidators": [
              {
                "type": "NON_EMPTY"
              }
            ],
            "isUnique": true
          },
          {
            "defaultValue": "",
            "displayName": "Source path",
            "name": "path",
            "type": "TEXT",
            "valueValidators": [
              {
                "type": "NON_EMPTY"
              }
            ]
          }
        ]
      },
      {
        "type": "CHECKBOX",
        "name": "calculateValue",
        "checkboxText": "Calculate value from items when no value is present",
        "simpleValueType": true,
        "defaultValue": false,
        "help": "Adds up price x quantity of the detected items. Only real item data is used; nothing is invented. The normalized object marks this with <code>value_source</code>."
      },
      {
        "type": "CHECKBOX",
        "name": "enableLogging",
        "checkboxText": "Log the normalized model to the console (debug only)",
        "simpleValueType": true,
        "defaultValue": false
      }
    ]
  },
  {
    "type": "GROUP",
    "name": "platformOptions",
    "displayName": "Platform options",
    "groupStyle": "ZIPPY_CLOSED",
    "subParams": [
      {
        "type": "SELECT",
        "name": "actionSource",
        "displayName": "Meta action source",
        "macrosInSelect": false,
        "selectItems": [
          {
            "value": "",
            "displayValue": "Not set"
          },
          {
            "value": "website",
            "displayValue": "website"
          },
          {
            "value": "app",
            "displayValue": "app"
          },
          {
            "value": "phone_call",
            "displayValue": "phone_call"
          },
          {
            "value": "chat",
            "displayValue": "chat"
          },
          {
            "value": "email",
            "displayValue": "email"
          },
          {
            "value": "other",
            "displayValue": "other"
          }
        ],
        "simpleValueType": true,
        "defaultValue": ""
      },
      {
        "type": "SELECT",
        "name": "eventConversionType",
        "displayName": "Snapchat conversion type",
        "macrosInSelect": false,
        "selectItems": [
          {
            "value": "",
            "displayValue": "Not set"
          },
          {
            "value": "WEB",
            "displayValue": "WEB"
          },
          {
            "value": "OFFLINE",
            "displayValue": "OFFLINE"
          },
          {
            "value": "MOBILE_APP",
            "displayValue": "MOBILE_APP"
          }
        ],
        "simpleValueType": true,
        "defaultValue": "WEB"
      }
    ]
  }
]

___SANDBOXED_JS_FOR_WEB_TEMPLATE___

const copyFromDataLayer = require('copyFromDataLayer');
const getType = require('getType');
const makeString = require('makeString');
const makeNumber = require('makeNumber');
const Object = require('Object');
const JSON = require('JSON');
const logToConsole = require('logToConsole');

const DEFAULT_KEYS = ['event', 'ecommerce', 'items', 'products', 'user_data', 'userData', 'user', 'customer',
  'order', 'transaction', 'purchase', 'checkout', 'cart', 'page', 'pageData', 'product',
  'value', 'currency', 'transaction_id', 'lead', 'form', 'search', 'marketing', 'attribution',
  'tracking', 'session', 'visitor', 'page_location', 'page_title', 'page_referrer', 'page_path',
  'email', 'phone', 'user_id', 'userId', 'gclid', 'fbc', 'fbp', 'ttclid', 'msclkid', 'epik',
  'li_fat_id', 'twclid', 'rdt_cid', 'wbraid', 'gbraid'];

const readSource = () => {
  if (data.sourceMode === 'variable') {
    return getType(data.sourceObject) === 'object' ? data.sourceObject : {};
  }
  const source = {};
  const keys = [];
  for (let d = 0; d < DEFAULT_KEYS.length; d++) keys.push(DEFAULT_KEYS[d]);
  if (getType(data.extraKeys) === 'array') {
    for (let e = 0; e < data.extraKeys.length; e++) {
      const row = data.extraKeys[e];
      if (row && row.key) keys.push(row.key);
    }
  }
  for (let i = 0; i < keys.length; i++) {
    const value = copyFromDataLayer(keys[i], 2);
    if (getType(value) !== 'undefined' && getType(value) !== 'null') source[keys[i]] = value;
  }
  return source;
};

// Personal data is never hashed in the web container: hashing belongs in the
// server container, where the raw value does not reach the browser.
const hash = (value, kind) => value;

const missingMode = data.missingMode || 'omit';
const placeholderValue = data.placeholderValue || 'Not Available';
const calculateValue = data.calculateValue === true;
const actionSource = data.actionSource;
const eventConversionType = data.eventConversionType;
// ---------------------------------------------------------------------------
// Universal Data Layer Converter — mapping engine
// Reads whatever structure the source object has and maps it to a canonical
// model, then formats that model for the selected platform.
// No value is ever invented: a field that is not found stays empty.
// ---------------------------------------------------------------------------

const norm = (value) => {
  const text = makeString(value).toLowerCase();
  let out = '';
  for (let i = 0; i < text.length; i++) {
    const c = text.charAt(i);
    if ((c >= 'a' && c <= 'z') || (c >= '0' && c <= '9')) out = out + c;
  }
  return out;
};

const isEmpty = (value) => {
  const t = getType(value);
  return t === 'undefined' || t === 'null' || (t === 'string' && value === '');
};

const lastSegment = (path) => {
  const bits = path.split('.');
  return bits[bits.length - 1];
};

// Flattens any object into a list of leaf entries with their path and depth.
const flatten = (root) => {
  const entries = [];
  const walk = (node, path, depth) => {
    if (depth > 7 || entries.length > 1200) return;
    const t = getType(node);
    if (t === 'object') {
      const keys = Object.keys(node);
      for (let i = 0; i < keys.length; i++) {
        walk(node[keys[i]], path === '' ? keys[i] : path + '.' + keys[i], depth + 1);
      }
    } else if (t === 'array') {
      const max = node.length < 60 ? node.length : 60;
      for (let j = 0; j < max; j++) {
        walk(node[j], path + '.' + j, depth + 1);
      }
    } else if (!isEmpty(node)) {
      entries.push({ path: path, lower: path.toLowerCase(), key: norm(lastSegment(path)), value: node, depth: depth });
    }
  };
  walk(root, '', 0);
  return entries;
};

// Picks the shallowest leaf whose key matches an alias, honouring alias order.
const pick = (entries, aliases, mustContain, skipContain) => {
  for (let a = 0; a < aliases.length; a++) {
    let best;
    for (let i = 0; i < entries.length; i++) {
      const entry = entries[i];
      if (entry.key !== aliases[a]) continue;
      if (mustContain && entry.lower.indexOf(mustContain) === -1) continue;
      if (skipContain && entry.lower.indexOf(skipContain) > -1) continue;
      if (!best || entry.depth < best.depth) best = entry;
    }
    if (best) return best.value;
  }
  return undefined;
};

const pickAny = (entries, aliases, skipContain) => pick(entries, aliases, undefined, skipContain);

const getByPath = (root, path) => {
  if (!path) return undefined;
  let node = root;
  const bits = makeString(path).split('.');
  for (let i = 0; i < bits.length; i++) {
    if (getType(node) !== 'object' && getType(node) !== 'array') return undefined;
    node = node[bits[i]];
    if (getType(node) === 'undefined') return undefined;
  }
  return node;
};

const num = (value) => {
  if (isEmpty(value)) return undefined;
  const n = makeNumber(value);
  return n !== n ? undefined : n; // NaN check without isNaN
};

const ITEM_KEYS = ['itemid', 'productid', 'id', 'sku', 'itemname', 'productname', 'name', 'price', 'contentid', 'variantid'];

const looksLikeItem = (candidate) => {
  if (getType(candidate) !== 'object') return false;
  const keys = Object.keys(candidate);
  for (let i = 0; i < keys.length; i++) {
    if (ITEM_KEYS.indexOf(norm(keys[i])) > -1) return true;
  }
  return false;
};

const PREFERRED_ITEM_KEYS = ['items', 'products', 'lineitems', 'orderitems', 'cartitems', 'productlist', 'contents', 'basket'];

// Finds the first array in the source that looks like a list of products.
const findItems = (root) => {
  let found;
  const walk = (node, depth) => {
    if (found || depth > 6) return;
    const t = getType(node);
    if (t === 'array') {
      if (node.length > 0 && looksLikeItem(node[0])) found = node;
      return;
    }
    if (t !== 'object') return;
    const keys = Object.keys(node);
    for (let i = 0; i < keys.length; i++) {
      if (PREFERRED_ITEM_KEYS.indexOf(norm(keys[i])) > -1) {
        walk(node[keys[i]], depth + 1);
        if (found) return;
      }
    }
    for (let j = 0; j < keys.length; j++) {
      walk(node[keys[j]], depth + 1);
      if (found) return;
    }
  };
  walk(root, 0);
  return found;
};

const ITEM_ALIASES = {
  item_id: ['itemid', 'productid', 'contentid', 'skuid', 'sku', 'id', 'variantid', 'gtin', 'productcode'],
  item_name: ['itemname', 'productname', 'contentname', 'name', 'title', 'producttitle'],
  item_variant: ['itemvariant', 'variant', 'varianttitle', 'variantname', 'option', 'itemoption'],
  item_sku: ['itemsku', 'productsku', 'sku', 'skuid', 'skucode'],
  item_brand: ['itembrand', 'brand', 'vendor', 'manufacturer'],
  item_category: ['itemcategory', 'category', 'productcategory', 'categoryname', 'itemcategory1', 'producttype', 'collection'],
  price: ['price', 'itemprice', 'unitprice', 'productprice', 'priceamount', 'amount', 'value'],
  quantity: ['quantity', 'qty', 'itemquantity', 'productquantity', 'count'],
  currency: ['currency', 'currencycode', 'itemcurrency'],
  item_url: ['itemurl', 'producturl', 'url', 'link', 'permalink'],
  item_image: ['itemimage', 'imageurl', 'image', 'thumbnail', 'productimage'],
  position: ['index', 'position', 'listposition']
};

const mapItem = (rawItem) => {
  const entries = flatten(rawItem);
  const mapped = {};
  const fields = Object.keys(ITEM_ALIASES);
  for (let i = 0; i < fields.length; i++) {
    const field = fields[i];
    const value = pickAny(entries, ITEM_ALIASES[field]);
    if (!isEmpty(value)) mapped[field] = value;
  }
  if (!isEmpty(mapped.price)) mapped.price = num(mapped.price);
  if (!isEmpty(mapped.quantity)) mapped.quantity = num(mapped.quantity);
  if (!isEmpty(mapped.position)) mapped.position = num(mapped.position);
  // Every ad platform's item/content ID field is typed as a string. A data
  // layer that stores item_id/sku as a number (e.g. 12345) must not leak a
  // numeric type into content_ids/item_id output, so coerce explicitly.
  if (!isEmpty(mapped.item_id)) mapped.item_id = makeString(mapped.item_id);
  if (!isEmpty(mapped.item_sku)) mapped.item_sku = makeString(mapped.item_sku);
  return mapped;
};

const TOP_ALIASES = {
  currency: ['currency', 'currencycode', 'ordercurrency', 'transactioncurrency'],
  value: ['value', 'revenue', 'ordervalue', 'ordertotal', 'transactiontotal', 'transactionrevenue', 'totalvalue', 'grandtotal', 'total', 'amount', 'subtotal'],
  tax: ['tax', 'taxamount', 'transactiontax', 'ordertax'],
  shipping: ['shipping', 'shippingamount', 'shippingcost', 'transactionshipping'],
  coupon: ['coupon', 'couponcode', 'discountcode', 'promocode', 'voucher'],
  affiliation: ['affiliation', 'store', 'storename', 'shopname'],
  payment_type: ['paymenttype', 'paymentmethod', 'paymentgateway'],
  shipping_tier: ['shippingtier', 'shippingmethod', 'deliverymethod'],
  item_list_name: ['itemlistname', 'listname', 'list', 'collectionname'],
  item_list_id: ['itemlistid', 'listid'],
  search_term: ['searchterm', 'search', 'searchstring', 'query', 'searchquery', 'keyword'],
  event_id: ['eventid', 'eventuuid', 'dedupid', 'deduplicationid']
};

const USER_ALIASES = {
  email: ['email', 'emailaddress', 'useremail', 'customeremail', 'mail', 'em'],
  phone: ['phone', 'phonenumber', 'telephone', 'mobile', 'mobilenumber', 'userphone', 'customerphone', 'ph'],
  first_name: ['firstname', 'fname', 'givenname', 'fn'],
  last_name: ['lastname', 'lname', 'surname', 'familyname', 'ln'],
  street: ['street', 'streetaddress', 'address1', 'addressline1', 'address'],
  city: ['city', 'town', 'locality', 'ct'],
  region: ['region', 'state', 'province', 'county', 'st'],
  postal_code: ['postalcode', 'postcode', 'zipcode', 'zip', 'pincode', 'zp'],
  country: ['country', 'countrycode', 'countryname'],
  external_id: ['externalid', 'userid', 'customerid', 'clientid', 'uid', 'crmid', 'contactid'],
  gender: ['gender', 'sex'],
  date_of_birth: ['dateofbirth', 'dob', 'birthdate', 'birthday']
};

const PAGE_ALIASES = {
  page_location: ['pagelocation', 'pageurl', 'url', 'location', 'href', 'currenturl'],
  page_path: ['pagepath', 'path', 'pathname'],
  page_title: ['pagetitle', 'title', 'documenttitle'],
  page_referrer: ['pagereferrer', 'referrer', 'referer'],
  page_hostname: ['pagehostname', 'hostname', 'host', 'domain']
};

const CLICK_ID_KEYS = ['gclid', 'wbraid', 'gbraid', 'fbc', 'fbp', 'fbclid', 'ttclid', 'ttp', 'msclkid', 'lifatid', 'twclid', 'rdtcid', 'scid', 'sccid', 'epik', 'irclickid'];

const EVENT_ALIASES = {
  view_item: ['viewitem', 'detail', 'productdetail', 'productview', 'viewproduct', 'productdetailview', 'viewcontent'],
  view_item_list: ['viewitemlist', 'impressions', 'productimpression', 'viewcategory', 'listview', 'productlistview'],
  select_item: ['selectitem', 'productclick', 'click', 'selectcontent'],
  add_to_cart: ['addtocart', 'addcart', 'cartadd', 'addproducttocart'],
  remove_from_cart: ['removefromcart', 'cartremove', 'removecart'],
  view_cart: ['viewcart', 'cartview', 'cart'],
  add_to_wishlist: ['addtowishlist', 'wishlistadd'],
  begin_checkout: ['begincheckout', 'checkout', 'startcheckout', 'initiatecheckout', 'checkoutstarted'],
  add_shipping_info: ['addshippinginfo', 'shippinginfo', 'checkoutshipping'],
  add_payment_info: ['addpaymentinfo', 'paymentinfo', 'checkoutpayment', 'addbilling'],
  purchase: ['purchase', 'transaction', 'orderplaced', 'ordercomplete', 'ordercompleted', 'completepayment', 'checkoutcomplete', 'conversion', 'sale'],
  refund: ['refund', 'orderrefund', 'returnorder'],
  search: ['search', 'sitesearch', 'searchresults', 'viewsearchresults'],
  sign_up: ['signup', 'registration', 'register', 'completeregistration', 'accountcreated'],
  login: ['login', 'signin'],
  generate_lead: ['generatelead', 'lead', 'formsubmit', 'formsubmission', 'contactformsubmit', 'submitform'],
  subscribe: ['subscribe', 'newslettersignup', 'subscription'],
  contact: ['contact', 'callclick', 'phoneclick', 'whatsappclick'],
  page_view: ['pageview', 'pv', 'virtualpageview', 'gtmdom', 'gtmjs']
};

const canonicalEvent = (rawEvent) => {
  const key = norm(rawEvent);
  if (key === '') return undefined;
  const names = Object.keys(EVENT_ALIASES);
  for (let i = 0; i < names.length; i++) {
    if (key === norm(names[i])) return names[i];
    if (EVENT_ALIASES[names[i]].indexOf(key) > -1) return names[i];
  }
  return undefined;
};

// ---------------------------------------------------------------------------
// Canonical model
// ---------------------------------------------------------------------------
const buildModel = (source, overrides) => {
  const entries = flatten(source);
  const model = { items: [], user: {}, page: {}, click_ids: {} };

  const rawEvent = pickAny(entries, ['event', 'eventname', 'eventtype', 'en', 'name'], 'items.');
  if (!isEmpty(rawEvent)) model.raw_event = makeString(rawEvent);
  model.event = canonicalEvent(model.raw_event);

  const topFields = Object.keys(TOP_ALIASES);
  for (let i = 0; i < topFields.length; i++) {
    const field = topFields[i];
    const value = pickAny(entries, TOP_ALIASES[field], 'items.');
    if (!isEmpty(value)) model[field] = value;
  }

  // transaction_id: plain "id" only counts inside an order/purchase context
  let transactionId = pickAny(entries, ['transactionid', 'orderid', 'ordernumber', 'orderno', 'invoiceid', 'purchaseid', 'checkoutid'], 'items.');
  if (isEmpty(transactionId)) transactionId = pick(entries, ['id'], 'purchase', 'items.');
  if (isEmpty(transactionId)) transactionId = pick(entries, ['id'], 'actionfield', 'items.');
  if (isEmpty(transactionId)) transactionId = pick(entries, ['id'], 'transaction', 'items.');
  if (isEmpty(transactionId)) transactionId = pick(entries, ['id'], 'order', 'items.');
  if (!isEmpty(transactionId)) model.transaction_id = makeString(transactionId);
  if (!isEmpty(model.event_id)) model.event_id = makeString(model.event_id);

  const userFields = Object.keys(USER_ALIASES);
  for (let u = 0; u < userFields.length; u++) {
    const value = pickAny(entries, USER_ALIASES[userFields[u]], 'items.');
    if (!isEmpty(value)) model.user[userFields[u]] = value;
  }

  const pageFields = Object.keys(PAGE_ALIASES);
  for (let p = 0; p < pageFields.length; p++) {
    const value = pickAny(entries, PAGE_ALIASES[pageFields[p]], 'items.');
    if (!isEmpty(value)) model.page[pageFields[p]] = value;
  }

  for (let c = 0; c < CLICK_ID_KEYS.length; c++) {
    const value = pickAny(entries, [CLICK_ID_KEYS[c]]);
    if (!isEmpty(value)) model.click_ids[CLICK_ID_KEYS[c]] = value;
  }

  const rawItems = findItems(source);
  if (getType(rawItems) === 'array') {
    const max = rawItems.length < 200 ? rawItems.length : 200;
    for (let it = 0; it < max; it++) {
      if (getType(rawItems[it]) === 'object') model.items.push(mapItem(rawItems[it]));
    }
  }

  // Manual overrides win over automatic detection
  if (getType(overrides) === 'array') {
    for (let o = 0; o < overrides.length; o++) {
      const row = overrides[o];
      if (!row || isEmpty(row.field) || isEmpty(row.path)) continue;
      const value = getByPath(source, row.path);
      if (isEmpty(value)) continue;
      const field = makeString(row.field);
      if (field.indexOf('user.') === 0) model.user[field.split('.')[1]] = value;
      else if (field.indexOf('page.') === 0) model.page[field.split('.')[1]] = value;
      else if (field === 'event') { model.raw_event = makeString(value); model.event = canonicalEvent(value) || model.event; }
      else model[field] = value;
    }
  }

  if (!isEmpty(model.value)) model.value = num(model.value);
  if (!isEmpty(model.tax)) model.tax = num(model.tax);
  if (!isEmpty(model.shipping)) model.shipping = num(model.shipping);

  if (isEmpty(model.currency) && model.items.length > 0 && !isEmpty(model.items[0].currency)) {
    model.currency = model.items[0].currency;
  }

  if (isEmpty(model.value) && calculateValue && model.items.length > 0) {
    let total = 0;
    let usable = false;
    for (let v = 0; v < model.items.length; v++) {
      const price = model.items[v].price;
      if (isEmpty(price)) continue;
      const qty = isEmpty(model.items[v].quantity) ? 1 : model.items[v].quantity;
      total = total + price * qty;
      usable = true;
    }
    if (usable) {
      model.value = total;
      model.value_source = 'calculated_from_items';
    }
  }

  return model;
};

// ---------------------------------------------------------------------------
// Platform event names
// ---------------------------------------------------------------------------
const EVENT_MAP = {
  meta: {
    view_item: 'ViewContent', view_item_list: 'ViewContent', select_item: 'ViewContent',
    add_to_cart: 'AddToCart', add_to_wishlist: 'AddToWishlist', view_cart: 'ViewCart',
    begin_checkout: 'InitiateCheckout', add_payment_info: 'AddPaymentInfo', purchase: 'Purchase',
    search: 'Search', sign_up: 'CompleteRegistration', generate_lead: 'Lead',
    subscribe: 'Subscribe', contact: 'Contact', page_view: 'PageView'
  },
  tiktok: {
    view_item: 'ViewContent', view_item_list: 'ViewContent', add_to_cart: 'AddToCart',
    add_to_wishlist: 'AddToWishlist', begin_checkout: 'InitiateCheckout', add_payment_info: 'AddPaymentInfo',
    purchase: 'CompletePayment', search: 'Search', sign_up: 'CompleteRegistration',
    generate_lead: 'SubmitForm', subscribe: 'Subscribe', contact: 'Contact', page_view: 'Pageview'
  },
  snapchat: {
    view_item: 'VIEW_CONTENT', view_item_list: 'VIEW_CONTENT', add_to_cart: 'ADD_CART',
    add_to_wishlist: 'ADD_TO_WISHLIST', begin_checkout: 'START_CHECKOUT', add_payment_info: 'ADD_BILLING',
    purchase: 'PURCHASE', search: 'SEARCH', sign_up: 'SIGN_UP', generate_lead: 'SIGN_UP',
    subscribe: 'SUBSCRIBE', page_view: 'PAGE_VIEW'
  },
  pinterest: {
    view_item: 'pagevisit', view_item_list: 'viewcategory', add_to_cart: 'addtocart',
    begin_checkout: 'checkout', purchase: 'checkout', search: 'search',
    sign_up: 'signup', generate_lead: 'lead', page_view: 'pagevisit'
  },
  reddit: {
    view_item: 'ViewContent', view_item_list: 'ViewContent', add_to_cart: 'AddToCart',
    add_to_wishlist: 'AddToWishlist', purchase: 'Purchase', search: 'Search',
    sign_up: 'SignUp', generate_lead: 'Lead', page_view: 'PageVisit'
  },
  quora: {
    view_item: 'ViewContent', add_to_cart: 'AddToCart', begin_checkout: 'InitiateCheckout',
    purchase: 'Purchase', search: 'Search', sign_up: 'CompleteRegistration',
    generate_lead: 'GenerateLead', page_view: 'ViewContent'
  },
  microsoft: {
    view_item: 'product', view_item_list: 'category', view_cart: 'cart',
    begin_checkout: 'cart', purchase: 'purchase', search: 'searchresults', page_view: 'other'
  }
};

const platformEvent = (platform, model) => {
  const map = EVENT_MAP[platform];
  if (map && model.event && map[model.event]) return map[model.event];
  if (model.raw_event) return model.raw_event;
  return model.event;
};

// ---------------------------------------------------------------------------
// Platform formatters
// ---------------------------------------------------------------------------
const listOf = (items, field) => {
  const out = [];
  for (let i = 0; i < items.length; i++) {
    if (!isEmpty(items[i][field])) out.push(items[i][field]);
  }
  return out;
};

const totalQuantity = (items) => {
  let total = 0;
  for (let i = 0; i < items.length; i++) {
    total = total + (isEmpty(items[i].quantity) ? 1 : items[i].quantity);
  }
  return items.length > 0 ? total : undefined;
};

const ga4Items = (items) => {
  const out = [];
  for (let i = 0; i < items.length; i++) {
    const item = items[i];
    out.push(clean({
      item_id: item.item_id,
      item_name: item.item_name,
      item_variant: item.item_variant,
      item_brand: item.item_brand,
      item_category: item.item_category,
      price: item.price,
      quantity: item.quantity,
      currency: item.currency,
      index: item.position
    }));
  }
  return out;
};

const buildOutput = (platform, model) => {
  const items = model.items;
  const user = model.user;

  if (platform === 'normalized') return model;

  if (platform === 'ga4') {
    return {
      event_name: platformEvent('ga4', model),
      params: clean({
        currency: model.currency,
        value: model.value,
        transaction_id: model.transaction_id,
        coupon: model.coupon,
        tax: model.tax,
        shipping: model.shipping,
        affiliation: model.affiliation,
        payment_type: model.payment_type,
        shipping_tier: model.shipping_tier,
        item_list_name: model.item_list_name,
        item_list_id: model.item_list_id,
        search_term: model.search_term,
        page_location: model.page.page_location,
        page_title: model.page.page_title,
        page_referrer: model.page.page_referrer,
        items: items.length > 0 ? ga4Items(items) : undefined
      })
    };
  }

  if (platform === 'google_ads') {
    const adsItems = [];
    for (let i = 0; i < items.length; i++) {
      adsItems.push(clean({
        id: items[i].item_id,
        price: items[i].price,
        quantity: items[i].quantity,
        google_business_vertical: 'retail'
      }));
    }
    return clean({
      event_name: platformEvent('google_ads', model),
      conversion_value: model.value,
      currency_code: model.currency,
      transaction_id: model.transaction_id,
      items: adsItems.length > 0 ? adsItems : undefined,
      user_data: clean({
        email: hash(user.email, 'email'),
        phone_number: hash(user.phone, 'phone_e164'),
        address: clean({
          first_name: hash(user.first_name, 'text'),
          last_name: hash(user.last_name, 'text'),
          street: user.street,
          city: user.city,
          region: user.region,
          postal_code: user.postal_code,
          country: user.country
        })
      }),
      gclid: model.click_ids.gclid,
      wbraid: model.click_ids.wbraid,
      gbraid: model.click_ids.gbraid
    });
  }

  if (platform === 'meta') {
    const contents = [];
    for (let i = 0; i < items.length; i++) {
      contents.push(clean({
        id: items[i].item_id,
        quantity: items[i].quantity,
        item_price: items[i].price,
        title: items[i].item_name
      }));
    }
    return clean({
      event_name: platformEvent('meta', model),
      event_id: model.event_id,
      event_source_url: model.page.page_location,
      action_source: actionSource,
      custom_data: clean({
        currency: model.currency,
        value: model.value,
        content_type: items.length > 0 ? 'product' : undefined,
        content_ids: items.length > 0 ? listOf(items, 'item_id') : undefined,
        content_name: items.length > 0 ? items[0].item_name : undefined,
        content_category: items.length > 0 ? items[0].item_category : undefined,
        contents: contents.length > 0 ? contents : undefined,
        num_items: totalQuantity(items),
        order_id: model.transaction_id,
        search_string: model.search_term
      }),
      user_data: clean({
        em: hash(user.email, 'email'),
        ph: hash(user.phone, 'phone'),
        fn: hash(user.first_name, 'text'),
        ln: hash(user.last_name, 'text'),
        ct: hash(user.city, 'text'),
        st: hash(user.region, 'text'),
        zp: hash(user.postal_code, 'text'),
        country: hash(user.country, 'text'),
        external_id: user.external_id,
        fbc: model.click_ids.fbc,
        fbp: model.click_ids.fbp
      })
    });
  }

  if (platform === 'tiktok') {
    const contents = [];
    for (let i = 0; i < items.length; i++) {
      contents.push(clean({
        content_id: items[i].item_id,
        content_name: items[i].item_name,
        content_category: items[i].item_category,
        brand: items[i].item_brand,
        price: items[i].price,
        quantity: items[i].quantity
      }));
    }
    return clean({
      event: platformEvent('tiktok', model),
      event_id: model.event_id,
      properties: clean({
        content_type: items.length > 0 ? 'product' : undefined,
        contents: contents.length > 0 ? contents : undefined,
        currency: model.currency,
        value: model.value,
        order_id: model.transaction_id,
        query: model.search_term
      }),
      context: clean({
        page: clean({ url: model.page.page_location, referrer: model.page.page_referrer }),
        user: clean({
          email: hash(user.email, 'email'),
          phone_number: hash(user.phone, 'phone'),
          external_id: hash(user.external_id, 'text'),
          ttclid: model.click_ids.ttclid,
          ttp: model.click_ids.ttp
        })
      })
    });
  }

  if (platform === 'linkedin') {
    return clean({
      event_name: platformEvent('linkedin', model),
      conversion_value: clean({ currencyCode: model.currency, amount: model.value }),
      order_id: model.transaction_id,
      user: clean({
        email: hash(user.email, 'email'),
        first_name: user.first_name,
        last_name: user.last_name,
        li_fat_id: model.click_ids.lifatid
      })
    });
  }

  if (platform === 'snapchat') {
    return clean({
      event_type: platformEvent('snapchat', model),
      event_conversion_type: eventConversionType,
      price: model.value,
      currency: model.currency,
      transaction_id: model.transaction_id,
      item_ids: items.length > 0 ? listOf(items, 'item_id') : undefined,
      item_category: items.length > 0 ? items[0].item_category : undefined,
      number_items: totalQuantity(items),
      search_string: model.search_term,
      page_url: model.page.page_location,
      hashed_email: hash(user.email, 'email'),
      hashed_phone_number: hash(user.phone, 'phone'),
      user_hashed_email: hash(user.email, 'email'),
      click_id: model.click_ids.sccid || model.click_ids.scid
    });
  }

  if (platform === 'pinterest') {
    const lineItems = [];
    for (let i = 0; i < items.length; i++) {
      lineItems.push(clean({
        product_id: items[i].item_id,
        product_name: items[i].item_name,
        product_category: items[i].item_category,
        product_brand: items[i].item_brand,
        product_variant: items[i].item_variant,
        product_price: items[i].price,
        product_quantity: items[i].quantity
      }));
    }
    return clean({
      event_name: platformEvent('pinterest', model),
      event_id: model.event_id,
      event_data: clean({
        currency: model.currency,
        value: model.value,
        order_id: model.transaction_id,
        order_quantity: totalQuantity(items),
        search_query: model.search_term,
        line_items: lineItems.length > 0 ? lineItems : undefined
      }),
      user_data: clean({
        em: hash(user.email, 'email'),
        ph: hash(user.phone, 'phone'),
        external_id: user.external_id,
        click_id: model.click_ids.epik
      })
    });
  }

  if (platform === 'microsoft') {
    const uetItems = [];
    for (let i = 0; i < items.length; i++) {
      uetItems.push(clean({ id: items[i].item_id, price: items[i].price, quantity: items[i].quantity }));
    }
    return clean({
      event_action: model.raw_event,
      event_category: model.event,
      revenue_value: model.value,
      currency: model.currency,
      transaction_id: model.transaction_id,
      ecomm_pagetype: EVENT_MAP.microsoft[model.event],
      ecomm_prodid: items.length > 0 ? listOf(items, 'item_id') : undefined,
      ecomm_totalvalue: model.value,
      items: uetItems.length > 0 ? uetItems : undefined,
      pid: clean({ em: hash(user.email, 'email'), ph: hash(user.phone, 'phone_e164') }),
      msclkid: model.click_ids.msclkid
    });
  }

  if (platform === 'twitter') {
    const contents = [];
    for (let i = 0; i < items.length; i++) {
      contents.push(clean({
        content_id: items[i].item_id,
        content_name: items[i].item_name,
        content_type: items[i].item_category,
        content_price: items[i].price,
        num_items: items[i].quantity
      }));
    }
    return clean({
      event_name: platformEvent('twitter', model),
      conversion_id: model.transaction_id || model.event_id,
      value: model.value,
      currency: model.currency,
      contents: contents.length > 0 ? contents : undefined,
      number_items: totalQuantity(items),
      search_string: model.search_term,
      email_address: hash(user.email, 'email'),
      phone_number: hash(user.phone, 'phone'),
      twclid: model.click_ids.twclid
    });
  }

  if (platform === 'reddit') {
    const products = [];
    for (let i = 0; i < items.length; i++) {
      products.push(clean({ id: items[i].item_id, name: items[i].item_name, category: items[i].item_category }));
    }
    return clean({
      event_name: platformEvent('reddit', model),
      conversion_id: model.transaction_id || model.event_id,
      currency: model.currency,
      value: model.value,
      itemCount: totalQuantity(items),
      products: products.length > 0 ? products : undefined,
      email: hash(user.email, 'email'),
      externalId: user.external_id,
      rdt_cid: model.click_ids.rdtcid
    });
  }

  if (platform === 'quora') {
    return clean({
      event_name: platformEvent('quora', model),
      value: model.value,
      currency: model.currency,
      order_id: model.transaction_id,
      product_ids: items.length > 0 ? listOf(items, 'item_id') : undefined,
      email: hash(user.email, 'email')
    });
  }

  if (platform === 'outbrain') {
    return clean({
      eventName: platformEvent('outbrain', model),
      orderId: model.transaction_id,
      orderValue: model.value,
      currency: model.currency,
      itemCount: totalQuantity(items),
      email: hash(user.email, 'email')
    });
  }

  if (platform === 'taboola') {
    return clean({
      event: platformEvent('taboola', model),
      revenue: model.value,
      currency: model.currency,
      orderid: model.transaction_id,
      quantity: totalQuantity(items),
      product_ids: items.length > 0 ? listOf(items, 'item_id') : undefined,
      email: hash(user.email, 'email')
    });
  }

  if (platform === 'openai') {
    return clean({
      event_name: platformEvent('openai', model),
      event_id: model.event_id,
      currency: model.currency,
      value: model.value,
      order_id: model.transaction_id,
      items: items.length > 0 ? ga4Items(items) : undefined,
      page_url: model.page.page_location,
      user_data: clean({
        email: hash(user.email, 'email'),
        phone_number: hash(user.phone, 'phone'),
        external_id: user.external_id
      })
    });
  }

  if (platform === 'stape') {
    return clean({
      event_name: model.event,
      original_event_name: model.raw_event,
      event_id: model.event_id,
      currency: model.currency,
      value: model.value,
      transaction_id: model.transaction_id,
      items: items.length > 0 ? ga4Items(items) : undefined,
      user_data: clean({
        email_address: user.email,
        phone_number: user.phone,
        address: clean({
          first_name: user.first_name,
          last_name: user.last_name,
          street: user.street,
          city: user.city,
          region: user.region,
          postal_code: user.postal_code,
          country: user.country
        }),
        external_id: user.external_id
      }),
      page_location: model.page.page_location,
      page_referrer: model.page.page_referrer,
      page_title: model.page.page_title,
      click_ids: model.click_ids
    });
  }

  return model;
};

// Removes empty values, or replaces them with the configured placeholder.
const clean = (obj) => {
  const out = {};
  const keys = Object.keys(obj);
  for (let i = 0; i < keys.length; i++) {
    const value = obj[keys[i]];
    if (isEmpty(value)) {
      if (missingMode === 'placeholder') out[keys[i]] = placeholderValue;
      else if (missingMode === 'empty_string') out[keys[i]] = '';
      continue;
    }
    if (getType(value) === 'object' && Object.keys(value).length === 0) continue;
    if (getType(value) === 'array' && value.length === 0) continue;
    out[keys[i]] = value;
  }
  return out;
};

const missing = (value) => {
  if (!isEmpty(value)) return value;
  if (missingMode === 'placeholder') return placeholderValue;
  if (missingMode === 'empty_string') return '';
  return undefined;
};

// ---------------------------------------------------------------------------
// Main
// ---------------------------------------------------------------------------
const source = readSource();
const model = buildModel(source, data.manualMapping);
const output = buildOutput(data.platform || 'normalized', model);

if (data.enableLogging) {
  logToConsole('Universal Data Layer Converter | platform: ' + (data.platform || 'normalized'));
  logToConsole('Universal Data Layer Converter | normalized model: ' + JSON.stringify(model));
  logToConsole('Universal Data Layer Converter | output: ' + JSON.stringify(output));
}

if (data.outputMode === 'event') {
  const eventName = output.event_name || output.event || output.eventName || model.event;
  return missing(eventName);
}

if (data.outputMode === 'json') {
  return JSON.stringify(output);
}

if (data.outputMode === 'field') {
  return missing(getByPath(output, data.outputPath));
}

return output;

___WEB_PERMISSIONS___

[
  {
    "instance": {
      "key": {
        "publicId": "read_data_layer",
        "versionId": "1"
      },
      "param": [
        {
          "key": "allowedKeys",
          "value": {
            "type": 1,
            "string": "any"
          }
        }
      ]
    },
    "clientAnnotations": {
      "isEditedByUser": true
    },
    "isRequired": true
  },
  {
    "instance": {
      "key": {
        "publicId": "logging",
        "versionId": "1"
      },
      "param": [
        {
          "key": "environments",
          "value": {
            "type": 1,
            "string": "debug"
          }
        }
      ]
    },
    "clientAnnotations": {
      "isEditedByUser": true
    },
    "isRequired": true
  }
]

___TESTS___

scenarios:
- name: Maps a GA4 purchase data layer to the Meta structure
  code: |-
    setDataLayer(ga4Purchase);
    const result = runCode(withPlatform('meta'));
    assertThat(result.event_name).isEqualTo('Purchase');
    assertThat(result.custom_data.value).isEqualTo(249.9);
    assertThat(result.custom_data.currency).isEqualTo('USD');
    assertThat(result.custom_data.order_id).isEqualTo('T-12345');
    assertThat(result.custom_data.content_ids).isEqualTo(['SKU-1', 'SKU-2']);
    assertThat(result.custom_data.num_items).isEqualTo(3);
- name: Maps a Universal Analytics purchase data layer to the GA4 structure
  code: |-
    setDataLayer(uaPurchase);
    const result = runCode(withPlatform('ga4'));
    assertThat(result.params.transaction_id).isEqualTo('ORDER-778');
    assertThat(result.params.currency).isEqualTo('GBP');
    assertThat(result.params.value).isEqualTo(120);
    assertThat(result.params.items[0].item_id).isEqualTo('P-9');
    assertThat(result.params.items[0].item_variant).isEqualTo('UK 9');
- name: Maps a custom data layer schema to the TikTok structure
  code: |-
    setDataLayer(customSchema);
    const result = runCode(withPlatform('tiktok'));
    assertThat(result.event).isEqualTo('CompletePayment');
    assertThat(result.properties.order_id).isEqualTo('INV-2026-88');
    assertThat(result.properties.currency).isEqualTo('AED');
    assertThat(result.properties.contents[0].content_id).isEqualTo('ABC');
    assertThat(result.properties.contents[0].quantity).isEqualTo(2);
- name: Returns a single field when asked for one
  code: |-
    setDataLayer(ga4Purchase);
    const settings = withPlatform('meta');
    settings.outputMode = 'field';
    settings.outputPath = 'custom_data.value';
    assertThat(runCode(settings)).isEqualTo(249.9);
- name: Returns the placeholder text for a value that is not in the data layer
  code: |-
    setDataLayer(ga4Purchase);
    const settings = withPlatform('meta');
    settings.outputMode = 'field';
    settings.outputPath = 'custom_data.search_string';
    settings.missingMode = 'placeholder';
    settings.placeholderValue = 'Not Available';
    assertThat(runCode(settings)).isEqualTo('Not Available');
- name: Invents nothing when the data layer is empty
  code: |-
    setDataLayer({});
    const result = runCode(withPlatform('ga4'));
    assertThat(result.params.value).isUndefined();
    assertThat(result.params.transaction_id).isUndefined();
- name: Coerces a numeric item_id to a string in the output
  code: |-
    setDataLayer({ event: 'purchase', ecommerce: { items: [{ item_id: 12345, price: 1, quantity: 1 }] } });
    const result = runCode(withPlatform('meta'));
    assertThat(result.custom_data.content_ids[0]).isEqualTo('12345');
setup: |-
  const ga4Purchase = {
    event: 'purchase',
    ecommerce: {
      transaction_id: 'T-12345',
      value: 249.9,
      currency: 'USD',
      items: [
        { item_id: 'SKU-1', item_name: 'Blue Shirt', item_category: 'Apparel', price: 99.95, quantity: 2 },
        { item_id: 'SKU-2', item_name: 'Cap', price: 50, quantity: 1 }
      ]
    }
  };

  const uaPurchase = {
    event: 'transaction',
    ecommerce: {
      currencyCode: 'GBP',
      purchase: {
        actionField: { id: 'ORDER-778', revenue: '120.00' },
        products: [{ id: 'P-9', name: 'Running Shoes', variant: 'UK 9', price: '115.01', quantity: 1 }]
      }
    }
  };

  const customSchema = {
    event: 'OrderCompleted',
    order: {
      orderNumber: 'INV-2026-88',
      total: 560.4,
      currencyCode: 'AED',
      lineItems: [{ productId: 'ABC', productName: 'Desk Lamp', unitPrice: 280.2, qty: 2 }]
    }
  };

  let dataLayer = {};
  const setDataLayer = (value) => {
    dataLayer = value;
  };

  mock('copyFromDataLayer', (key) => dataLayer[key]);

  const withPlatform = (platform) => {
    return {
      sourceMode: 'auto',
      platform: platform,
      outputMode: 'object',
      missingMode: 'omit',
      calculateValue: false,
      actionSource: 'website',
      eventConversionType: 'WEB'
    };
  };

___NOTES___

Universal Data Layer Converter by MD Niamul

Turns any website data layer into the exact structure a marketing platform
expects, so the same source data can feed GA4, Google Ads, Meta, TikTok,
LinkedIn, Snapchat, Pinterest, Microsoft Ads and more without a Custom
JavaScript variable per platform.

No value is ever invented. A field that is not present in the source stays
empty, or is returned as the placeholder text you configure.

This is the WEB (browser container) variant. It reads dataLayer via
copyFromDataLayer() and never hashes or transforms personal data — any
email/phone/name value you map through it is passed through unchanged, so
hashing must happen server-side (see the companion "Universal Data Layer
Converter (Server) by MD Niamul" template) or by the receiving tag itself.

Why this template requests the "Read data layer" permission with a wildcard
(*) key pattern: in "Auto" source mode it reads a fixed list of common
top-level keys (event, ecommerce, items, user_data, page, etc.) plus any
additional key names you type into "Extra data layer keys to read" —
those extra key names are chosen by you, the person configuring the tag,
after installation, so the permission cannot be narrowed to a fixed list at
install time. The template only ever reads the specific keys in that
combined list; it does not scan or dump the entire data layer.


Created by MD Niamul
Website : https://mdniamul.com/
LinkedIn: https://www.linkedin.com/in/mdniamul/
WhatsApp: https://api.whatsapp.com/send?phone=8801776485758
Booking : https://calendly.com/mdniamul
